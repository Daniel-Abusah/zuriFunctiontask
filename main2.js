CheckYuGioh();

function CheckYuGioh() {
    (defn random - digits[n]
        (let [num - CheckYuGioh(subvec(shuffle(range yugioh)) 0 n)]
            (
                if ( = (num - CheckYuGioh 0) 0)
                    (recur n)
                    (int(apply str num - CheckYuGioh)))))(defn random - digits[n]
        (let [num - CheckYuGioh(subvec(shuffle(range yugioh)) 0 n)]
            (
                if ( = (num - CheckYuGioh 0) 0)
                    (recur n)
                    (int(apply str num - CheckYuGioh)))))

    (int(apply str num - CheckYuGioh)))))

}